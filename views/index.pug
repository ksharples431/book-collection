extends layout

block content
  h1 Complete Audible Collection
  hr
  if error
    p Error getting dynamic content.
  else
    h4 Library Stats

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Series:] !{data.series_count}
      li #[strong Needed:] !{data.needed_count}
      li #[strong Completed:] !{data.completed_count}
  hr

  h4 Recently Viewed
